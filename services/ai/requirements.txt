# Levant AI — AI/aggregation service (Python, FastAPI)
#
# v1 runtime deps only. All of these ship prebuilt wheels for Python 3.12–3.14,
# so pip never invokes a compiler — important on a low-RAM box (source builds
# of numpy/pandas OOM here). Floor pins (not ==) let pip pick a wheel that
# matches the installed Python.
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.10,<3
pydantic-settings>=2.6,<3
openai>=1.0
anthropic>=0.69

# Heavier quant / broker / crypto deps are deferred to requirements-optional.txt.
# They compile from source on Python 3.14 and will OOM a small VPS — install
# them only when the read-only aggregation/quant features land, ideally on a
# Python version with prebuilt wheels (3.12) or after adding swap.
