# Levant AI — AI/aggregation service (Python, FastAPI)
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
anthropic==0.69.0

# Read-only broker aggregation + quant (used as the service grows)
ccxt==4.4.40
pandas==2.2.3
numpy==2.1.3

# Encryption for broker API keys at rest (AES-256 via Fernet)
cryptography==44.0.0
