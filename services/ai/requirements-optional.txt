# Deferred deps for read-only broker aggregation, quant, and secret encryption.
# NOT needed for v1 and NOT installed by default.
#
# WARNING: on Python 3.14 numpy/pandas have no prebuilt wheels yet and compile
# from source — that needs ~1-2 GB free RAM and will OOM a small VPS (it did on
# the first deploy attempt). Before installing these, EITHER:
#   - add swap (see deploy/README.md), or
#   - use a Python 3.12 venv where prebuilt wheels exist, or
#   - install on a build box and ship the venv.
#
# Install with:  .venv/bin/pip install -r requirements-optional.txt
ccxt>=4.4
pandas>=2.2
numpy>=2.1
cryptography>=44
